{
  "name": "travel-map-agent",
  "version": "2.0.0",
  "description": "旅行规划 Agent：对话式规划（v2.0 变体 A）——多轮自然语言收集约束 → 自动填表 → 复用现有 plan/replan 直接出路书；对话维度内存埋点（schema 2.0.0）；跨会话记忆/回放按实施方案 §0 PENDING（依赖持久化存储）。",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/planner.test.js tests/llm.test.js tests/agent-planner.test.js tests/location-data.test.js tests/verifier.test.js tests/repair.test.js tests/state-machine.test.js tests/tracer.test.js tests/day-plan.test.js tests/scoring.test.js tests/tools.test.js tests/multi-hotel.test.js tests/replan.test.js tests/day-estimate.test.js tests/metrics.test.js tests/eval-set.test.js tests/intake.test.js tests/dialog.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}

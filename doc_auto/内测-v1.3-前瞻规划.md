# 内测 v1.3 前瞻规划：显式状态机 + 自动修复闭环

> 创建日期：2026-08-06
> 实现日期：2026-08-10
> 状态：**P0 + P1 + P2 已实现**（实现落地记录见 §11）
> 基线版本：[`内测-v1.2-改进规划.md`](./内测-v1.2-改进规划.md)（其上游为 [`内测-v1.1.0.md`](./内测-v1.1.0.md)）
> 终态参照：[`map-travel-agent-终极构想.md`](./map-travel-agent-终极构想.md)

本版本是从"单轮规划器"迈向"多轮自修复 Agent"的**地基版本**。v1.4（策略引擎）、v1.5（工具/校验扩展）、v1.6（记忆/局部重算）均依赖本版本落地的状态机与修复动作库。

---

## 1. 版本定位

| 维度 | v1.2（前置） | v1.3（本版本） |
|------|-------------|----------------|
| 流程形态 | 隐式串行调用，边界不清 | 显式状态机 `collect → build → plan → verify → repair → finalize / fallback` |
| 失败处理 | 以 warning 为主，部分裁剪 | 失败分类 → 触发修复动作 → 重跑校验 → 收敛 |
| 可靠性 | 基础错误处理 | 轮次上限 + 改善阈值 + 保底回退 + 全链路 trace 埋点 |

一句话目标：**把隐式流程升级为可收敛、可回退、可复盘的状态机，让"校验失败"从提示变成可自动执行的修复动作。**

---

## 2. 前瞻规划（本版本要实现的内容）

### 2.1 P0（必须）

1. **显式状态机落地**
   - 状态集：`collect_input → build_context → plan_initial → verify → repair → finalize`，异常兜底态 `fallback`。
   - 每个状态明确定义：进入条件、执行动作、退出条件、失败处理。
   - 状态流转由后端统一调度器驱动（`state-machine.js` 或等价模块），不再散落在 `server.js` 的顺序调用里。

2. **修复动作库（Repair Actions）**
   - 至少实现 4 类可执行修复动作：
     - `split_day`（拆天）：单日超载时把景点拆到相邻天；
     - `drop_lowest_priority`（降优先级删点）：删除优先级最低景点；
     - `swap_neighbor`（就近替换/换序）：调整相邻点顺序以减少折返；
     - `merge_day`（并天）：用户天数偏多时压缩空天。
   - 每个修复动作输入当前 `planData` + 失败原因，输出新的 `planData` + 修复说明。

3. **收敛控制（防死循环）**
   - 轮次上限 `MAX_REPAIR_ROUNDS`（默认 3）；
   - 改善阈值：连续 N 轮校验分数无改善则停止（默认 N=2）；
   - 失败分类回退：无法修复时进入 `fallback`，输出"保底可执行方案 + 明确未满足项"，而非报错终止。

4. **全链路 trace 埋点（本版本必须提前埋点）**
   - 每次状态流转、工具调用、校验结果、修复动作均写入结构化 trace（详见 §4）。
   - 埋点数据落地为可读取结构（内存 + 可选 JSON 落盘），供 v1.7 可观测体系直接消费。

### 2.2 P1（应做）

1. **校验结果结构化**
   - 校验器输出统一 `{ code, level, message, evidence }` 结构，`level ∈ {info, warn, error}`；
   - 每条校验结论必须带机器可判定的 `pass/fail` 与证据引用（geocode 结果、时长估算等）。

2. **修复链路差异记录**
   - 每轮修复前后记录 diff（哪些点被删/移动/换序），供最终"为什么改了路线"复盘。

### 2.3 P2（可选）

1. **状态机可视化调试端点**
   - 新增 `GET /api/debug/last-trace`，返回最近一次规划的完整 trace（仅内测环境开启）。

---

## 3. 关键方案设计

### 3.1 状态机定义

| 状态 | 进入条件 | 执行动作 | 退出条件 | 失败处理 |
|------|----------|----------|----------|----------|
| `collect_input` | 收到请求 | 归一化 destinations/places/lodging/约束 | 输入合法 | 输入非法 → `fallback` |
| `build_context` | 输入合法 | 预解析 geocode、构建酒店锚点、注入历史偏好（v1.6 接入） | 上下文就绪 | 关键 geocode 全失败 → `fallback` |
| `plan_initial` | 上下文就绪 | LLM + 工具生成候选顺序与 dailyPlans | 产出初版计划 | LLM/工具异常 → 重试或 `fallback` |
| `verify` | 有计划 | 运行校验器集合，输出结构化结论 | 全部 pass → `finalize`；有 error → `repair` | 校验器异常 → `fallback` |
| `repair` | 存在可修复 error | 选择并执行修复动作，重跑 verify | 收敛或达上限 | 达上限未收敛 → `fallback` |
| `finalize` | 校验通过 | 组装统一输出 schema | 输出完成 | — |
| `fallback` | 任一致命失败 | 输出保底方案 + 明确未满足项 | 输出完成 | — |

### 3.2 修复决策器（Repair Router）

- 输入：`verify` 输出的失败列表（按 code 分类）；
- 路由规则（示例）：
  - `TIME_OVERLOAD`（单日超载）→ `split_day` 或 `drop_lowest_priority`；
  - `CROSS_CITY_CONFLICT`（跨城冲突）→ `swap_neighbor` / 重新分天；
  - `HOTEL_LOOP_BROKEN`（酒店闭环断裂）→ 强制注入酒店腿或 `split_day`；
  - `TOO_MANY_EMPTY_DAYS`（空天过多）→ `merge_day`。
- 每轮只执行"预期收益最高"的一个修复动作，重跑校验后再决策，保证可追踪。

### 3.3 收敛与回退

```
round = 0
while round < MAX_REPAIR_ROUNDS:
    result = verify(plan)
    if result.pass: return finalize(plan)
    if no_improvement_for(N rounds): break
    plan = repair(plan, result.failures)
    round += 1
return fallback(plan, unresolved=result.failures)
```

---

## 4. 埋点设计（提前埋点，全链路 trace）

> 本版本是全项目埋点基线。后续所有版本的埋点都在此 schema 上扩展，v1.7 可观测体系直接消费。

### 4.1 统一 trace 事件结构

```json
{
  "traceId": "uuid",
  "requestId": "uuid",
  "ts": 1234567890,
  "stage": "verify",
  "eventType": "state_enter | state_exit | tool_call | validation | repair_action | fallback",
  "payload": { },
  "durationMs": 0,
  "status": "ok | warn | error"
}
```

### 4.2 必须埋的事件点（v1.3 基线）

| 事件点 | eventType | 关键 payload | 用途 |
|--------|-----------|--------------|------|
| 状态进入/退出 | `state_enter`/`state_exit` | `stage`、`durationMs` | 各阶段耗时、流转路径 |
| 工具调用 | `tool_call` | `tool`、`args摘要`、`ok/fail`、`durationMs`、`cacheHit` | 工具成功率、延迟、成本 |
| 校验结论 | `validation` | `code`、`level`、`pass`、`evidenceRef` | 违例率、失败类型分布 |
| 修复动作 | `repair_action` | `action`、`reason`、`beforeScore`、`afterScore`、`diff` | 修复收敛效果 |
| 回退 | `fallback` | `unresolved[]`、`reason` | 兜底触发率 |

### 4.3 埋点落地要求

- 埋点写入不得阻塞主流程（异步/批量）；
- 埋点失败**必须记录日志**，不得静默吞掉（遵循"无静默失败"约束）；
- 每次请求产出一条可导出的 trace（内存保留最近 N 条，内测可 JSON 落盘）。

---

## 5. 评价指标（验收标准，用于判定是否满足预期）

> 指标基于固定回归样例集（见 §6 检查清单）。v1.3 首次建立样例集与基线数值。

### 5.1 功能性指标

| 指标 | 定义 | 目标值 |
|------|------|--------|
| 状态机覆盖率 | 每次请求都完整走过定义态且无未定义跳转 | 100% |
| 修复动作可执行率 | 触发修复时能产出合法新 planData 的比例 | ≥ 95% |
| 收敛成功率 | 存在可修复 error 的样例最终收敛到 pass 的比例 | ≥ 80% |
| 兜底可用率 | 进入 fallback 时仍输出可执行保底方案的比例 | 100% |

### 5.2 可靠性指标

| 指标 | 定义 | 目标值 |
|------|------|--------|
| 死循环发生率 | 超过 MAX_REPAIR_ROUNDS 仍未终止的比例 | 0% |
| 平均修复轮次 | 收敛样例的平均 repair 轮数 | ≤ 2 |
| trace 完整率 | 请求产出完整 trace（含所有关键事件）的比例 | 100% |
| 无静默失败 | 埋点/工具/校验异常均有日志记录 | 100% |

### 5.3 一致性指标

| 指标 | 定义 | 目标值 |
|------|------|--------|
| 输出一致性 | `recommendedOrder`/`dailyPlans`/`roadbook` 三者景点集合一致 | 100% |
| 校验-结果一致性 | validation 结论与最终 dailyPlans 不矛盾 | 100% |

### 5.4 验收结论判定

- **通过**：5.1、5.2、5.3 全部达标，且回归样例集测试 100% 通过；
- **不通过**：任一 P0 指标未达标，需回炉修复后重测。

---

## 6. 开发前检查清单

- [ ] 确认状态集与流转图（评审通过）
- [ ] 确认修复动作库最小集（split/drop/swap/merge）
- [ ] 确认收敛参数默认值（MAX_REPAIR_ROUNDS=3，无改善阈值 N=2）
- [ ] 建立回归样例集（跨国跨城、同城高密、无酒店、超载 2 天 6 点等）并记录基线数值
- [ ] 确认 trace 埋点 schema（供 v1.7 复用）
- [ ] 补充测试：`tests/state-machine.test.js`、`tests/repair.test.js`、扩展 `tests/agent-planner.test.js`
- [ ] 通过后进入开发并更新 `changes.md`

---

## 7. 风险与边界

1. 状态机重构可能影响现有同步/流式接口，需保证对外契约兼容（`plan` / `plan/stream`）；
2. 修复动作可能与"用户明确指定顺序"冲突，需保留用户强约束优先级；
3. 埋点数据量在高频调用下需控制体积（摘要化 payload）；
4. 收敛阈值需用真实样例调参，过严会误兜底，过松会浪费轮次。

---

## 8. 关键任务拆解（难点分解）

> 只拆解有难度、易踩坑的任务；简单任务（字段增删、文案）不在此列。每个难点给出子步骤 + 关键实现点 + 完成判据。

### 8.1 难点：显式状态机落地（最高优先）

**子步骤**
1. 抽象状态机内核：定义 `State` 枚举、`Transition(from, to, guard, action)`、统一 `Context` 对象（贯穿全流程携带输入/中间结果/trace）。
2. 实现调度器 `runStateMachine(context)`：循环取当前态 → 执行 action → 依 guard 决定下一态，直到 `finalize`/`fallback`。
3. 将现有 `server.js` 顺序逻辑逐态迁移（先照搬为各态 action，不改行为），保证对外接口 `plan` / `plan/stream` 契约不变。
4. 流式接口对接：每次 `state_enter/exit` 映射为一个 `progress` 事件，替换现有硬编码进度。
5. 回归对比：同一批样例，重构前后输出 diff 应为空（纯结构重构）。

**关键实现点**
- Context 单一数据源，禁止各态私存全局变量；
- guard 必须是纯函数（可单测）；
- 未定义的状态跳转直接抛错并进 `fallback`，不允许"隐式跳过"。

**完成判据**：状态机覆盖率 100%，重构前后输出零 diff，流式进度由状态流转驱动。

### 8.2 难点：修复动作库（每个动作单独拆）

**通用契约**：`repair(plan, failure, context) -> { newPlan, changeLog }`，纯函数、可单测、幂等。

| 动作 | 子步骤 | 完成判据 |
|------|--------|----------|
| `split_day` | ① 定位超载日 ② 选切分点（按顺序/就近）③ 生成新日并重排酒店腿 | 拆后每日不超载且景点不丢 |
| `drop_lowest_priority` | ① 计算各点优先级 ② 选最低且可删项 ③ 移除并记录到替代方案 | 删点后可行，被删点进 alternatives |
| `swap_neighbor` | ① 计算相邻交换的折返收益 ② 择优交换 ③ 复算段间时长 | 交换后折返程度不升高 |
| `merge_day` | ① 识别空/轻载日 ② 合并到相邻日 ③ 校验合并后不超载 | 天数压缩且不产生新超载 |

**关键实现点**：每个动作只做"一件事"，组合由决策器负责；所有动作产出 `changeLog` 供 diff 复盘。

**完成判据**：修复动作可执行率 ≥ 95%，每个动作有独立单测覆盖正常/边界。

### 8.3 难点：修复决策器 + 收敛控制

**子步骤**
1. 失败分类：把 verify 输出按 `code` 归类，映射到候选修复动作集合。
2. 收益预估：对候选动作预估 `afterScore`（可用轻量估算，不必真跑全量）。
3. 单步择优：每轮只执行收益最高的一个动作，重跑 verify。
4. 收敛判定：实现 `no_improvement_for(N)` 与 `MAX_REPAIR_ROUNDS`，命中即停并进 `fallback`。
5. 死循环防护测试：构造"无论怎么修都不可行"的样例，验证一定在上限内终止。

**关键实现点**：`beforeScore/afterScore` 用统一评分函数（与 v1.4 打分器共用接口，提前预留）；避免同一动作在两轮间来回抖动（记录已尝试动作指纹）。

**完成判据**：死循环发生率 0%，平均修复轮次 ≤ 2，收敛成功率 ≥ 80%。

### 8.4 难点：全链路 trace 埋点（供后续版本复用，必须一次做对）

**子步骤**
1. 定义 trace 写入器 `tracer`：`tracer.emit(event)`，内部异步缓冲 + 批量落地。
2. 在状态机内核统一织入 `state_enter/exit`（AOP 式，避免每态手写）。
3. 工具调用统一包裹 `withTrace(toolCall)`，自动记录 `durationMs/ok/cacheHit`。
4. 校验器与修复动作在返回处 emit `validation` / `repair_action`。
5. 提供 `getTrace(requestId)` 与内测落盘开关。

**关键实现点**：payload 摘要化（大对象存引用/裁剪）；埋点异常必须 `logger.warn` 不得静默吞（遵守无静默失败约束）；schema 版本号字段 `schemaVersion`，为 v1.7 演进留位。

**完成判据**：trace 完整率 100%，埋点开销对端到端延迟影响 ≤ 5%（v1.7 复核）。

---

## 9. 建议实施顺序（本版本内）

1. 先做 8.1 状态机内核 + 迁移（不改行为，纯重构，先绿测试）；
2. 再做 8.4 埋点织入（趁重构一并埋，避免二次改动）；
3. 然后 8.2 修复动作库（逐个动作 + 单测）；
4. 最后 8.3 决策器与收敛（把动作串成闭环）；
5. 全程用 §6 回归样例集守护，避免行为回退。

---

*本文档随讨论迭代；正式立项后进入 v1.3 开发。*

---

## 11. 实现落地记录（2026-08-10）

本轮实现了 **P0 + P1 + P2 全部条目**，对外接口契约（`POST /api/agent/plan`、`/api/agent/plan/stream`）保持兼容，新增字段均为**增量**，无收敛需求时输出与 v1.2 等价。

### 11.1 新增模块

| 模块 | 职责 | 关键导出 |
|------|------|----------|
| `state-machine.js` | 通用状态机引擎（统一调度器） | `runStateMachine`（含非法跳转/环路防护/tracer 织入） |
| `tracer.js` | 全链路 trace 埋点基线（schema v1.3.0） | `createTracer`、`recordTrace`、`getLastTrace`、`withTrace`、`summarizePayload` |
| `verifier.js` | 结构化校验器 | `runVerifiers` → `{ pass, score, findings[{code,level,message,evidence,pass}] }` |
| `repair.js` | 修复动作库 + 决策器 + 收敛控制 | `splitDay`/`dropLowestPriority`/`swapNeighbor`/`mergeDay`、`applyRepair`、`chooseRepairAction`、`shouldStopRepair`、`noImprovement` |

### 11.2 状态机落地（对应 §2.1-1、§8.1）

- `server.js` 的 `buildAgentPlanPayload` 由「顺序调用」重构为状态机驱动：`build_context → plan_initial → verify → repair → finalize / fallback`，`verify ↔ repair` 为真实收敛环，由 `runStateMachine` 统一调度。
- 请求级前置校验（缺参/空景点）仍返回 **400**（属 API 契约，不进状态机兜底）；规划阶段的不可收敛才进 `fallback`。
- 单一数据源 `ctx` 贯穿全流程；`state_enter/exit` 由引擎 AOP 式统一织入 tracer，未定义跳转直接抛错，`maxTransitions=40` 防环路。

### 11.3 修复动作库与决策/收敛（对应 §2.1-2、§2.1-3、§8.2、§8.3）

- 4 类修复动作均为纯函数（深拷贝输入、可单测、产出 `changeLog`）：`split_day`（拆天）、`drop_lowest_priority`（删点并入替代方案）、`swap_neighbor`（按城市聚类换序）、`merge_day`（并天）。
- 决策器 `chooseRepairAction` 按严重度 `TIME_OVERLOAD > HOTEL_LOOP_BROKEN > CROSS_CITY_CONFLICT > TOO_MANY_EMPTY_DAYS` 路由，每轮只执行一个动作；单日仅 1 点的超载自动改走删点。
- 收敛控制 `shouldStopRepair`：`MAX_REPAIR_ROUNDS`（默认 3）+ `NO_IMPROVE_LIMIT`（默认 2），命中即进 `fallback` 输出保底方案 + `unresolved` 未满足项。参数可由环境变量覆盖。

### 11.4 结构化校验与埋点（对应 §2.2-1、§4、§8.4）

- 校验结论统一 `{ code, level, message, evidence, pass }`，`level ∈ {info, warn, error}`；`error` 阻断收敛，`warn`（如空天）不阻断。
- trace 事件遵循 §4.1 schema（`traceId/requestId/ts/stage/eventType/payload/durationMs/status` + `schemaVersion`）；`payload` 摘要化避免体积失控；埋点异常 `logger.warn` 记录，**不静默吞掉**。
- 每次请求（含异常）在 `finally` 中 `recordTrace`，内存保留最近 20 条，供 §2.3 的 `GET /api/debug/last-trace`（`ENABLE_DEBUG_TRACE` 开关，内测默认开）复盘。

### 11.5 修复链路差异记录（对应 §2.2-2）

- 每个修复动作返回 `changeLog`（动作、受影响天、移动/删除项、before/after 顺序、note），汇总进 `validation.repairSummary.changeLogs`；被删景点进 `validation.excludedPlaces` 与替代方案「补齐被自动删减的景点」。

### 11.6 测试与回归

- 新增 `tests/state-machine.test.js`、`tests/repair.test.js`、`tests/verifier.test.js`、`tests/tracer.test.js`；`package.json` test 脚本纳入四份新测试。
- 全量 `node --test` 通过 **60/60**（原 31 + 新增 29）。
- 手工 smoke：`/api/strategies` 200、`/api/debug/last-trace` 空态 404 / 有请求后返回完整 trace、缺参返回 400 契约、静态资源 200；状态机异常路径（LLM 不可达）确认 `state_enter/exit(error)` 正确埋点且经 `finally` 落盘。
- 死循环防护：`state-machine.test.js` 构造「恒不收敛」样例，验证在 `MAX_REPAIR_ROUNDS` 内终止于 `fallback`。

### 11.7 与后续版本的衔接

- trace schema（`schemaVersion=1.3.0`）为全项目埋点基线，v1.4~v1.6 在其上扩展字段，v1.7 可观测体系直接消费；
- `verify/repair` 的评分接口（`score` 越低越好）为 v1.4 打分器预留统一口径；
- `evidence` 证据引用为 v2.0「结论证据绑定」预留结构。

---

*本文档随讨论迭代；P0/P1/P2 已于 2026-08-10 落地，版本号定为「内测 v1.3.0」。*

---

## 12. 路书体验重构与天数冲突决策（v1.3.1，2026-08-10）

在 v1.3.0 状态机/修复闭环基础上，针对内测反馈的三个体验问题做了重构（属 v1.3 小版本，不改状态机内核）：

### 12.1 问题定位

- **矛盾根因是「结构信息有两个数据源」**：`roadbook`（LLM 早期自由步骤，处理前）与 `dailyPlans`（后端确定性，处理后）并存且未和解，导致「行程概述 / 路线策略 / 按日行程」互相打架。
- **替代方案负价值**：旧 `alternativeProposals`（自动 A/B + 补齐删减 + LLM 幻觉）内容生硬、与正文重复。
- **天数被后端替用户拍板**：`estimateNaturalDaysAndSubset` 静默压缩/删点，用户无选择权。

### 12.2 天数冲突决策树（唯一权威结构以后端为准）

设 `d=系统估算天数`、`r=用户天数`、`gap=|d-r|`：

| 情况 | 处理 |
|------|------|
| `gap==0` | 单一方案（r 天） |
| `gap>1` | 以 LLM 为主，单一方案（d 天），**不再静默删点** |
| `gap==1` | 给两套完整方案：方案A（你的 r 天，`d>r` 时删点并标注）+ 方案B（建议 d 天） |

实现：`server.js` 新增可测纯函数 `decideDayPlan`；主方案进状态机做校验/修复，第二方案由 `assembleResult` 用 `secondarySpec` 确定性构建（`alternativePlan` 含 dailyPlans/metrics/findings）。

### 12.3 路书四段式（单一数据源）

①概览头（数据驱动事实条 + 一句受约束点评 + 天数冲突提示，合并原行程概述/路线策略/住宿摘要）→ ②景点速览（保留）→ ③按日行程（合并原 LLM `roadbook` 与 `dailyPlans`，仅用权威 `dailyPlans` + 车站卡片排版；`gap==1` 时第二方案上下堆叠）→ ④校验与提醒（注意事项折叠）。删除 LLM 自由文本路书与旧替代方案区块。

### 12.4 输出 schema（增量、兼容）

新增 `planLabel`、`dayConflict{type,d,r,message}`、`alternativePlan`；`alternativeProposals` 置空；被删景点在 `excludedPlaces` 标注归属方案。

### 12.5 测试

新增 `tests/day-plan.test.js`（5 例覆盖决策树各分支）；`server.js` 以 `require.main===module` 守卫 listen 并导出 `decideDayPlan` 便于单测；全量 `node --test` 通过（65/65）。

---

## 13. 已知待解决问题（Open Issues，2026-08-10 登记）

> 内测反馈发现的、当前版本与后续已规划版本**均未明确解决**的问题，登记于此作为待办，避免遗漏。

### OI-1：分天未按城市聚类，导致同城景点被拆到不同天、每日无谓跨城

**现象**（哥本哈根 + 马尔默，2 天，方案A）：

- Day1：小美人鱼(哥本哈根) → 马尔默竞技场(马尔默) → 回酒店；
- Day2：市政厅(哥本哈根) → 利姆港(马尔默) → 回酒店；
- 两天都跨厄勒海峡往返。理想应为 Day1 全哥本哈根、Day2 全马尔默，**同日跨城可降到 0**。

**根因**：

1. `agent-planner.buildPlanDataFromOrder` 用 `buckets[index % days]` **轮询分桶（round-robin）**，把顺序中相邻（常为同城）的景点交错分配到不同天；即使顺序已按城市聚类，分天后仍被打散。
2. 打分与校验作用在**线性顺序**上（`computeRouteMetrics` 跑的是 `recommendedOrder`），**看不到「每日回酒店」的天边界**，优化的是整条链路而非「每日分组后的跨城次数」。
3. `verifier` 的 `CROSS_CITY_CONFLICT` 阈值为**单日 ≥ 2 次**切换；轮询造成的是「每天恰好 1 次」不必要跨城，低于阈值 → 不产生 finding → 不触发 `swap_neighbor` / 重新分天修复。

**后续版本覆盖情况**：

- v1.4（策略引擎）仅在**顺序生成**层做「先按城市分组」（§8.1），**未改分天分桶**，也未把打分对象改为「按日分组」，因此无法保证分天后仍聚类；
- v1.6 仅有「跨日边界轻校验」，非重排分天；
- `map-travel-agent-终极构想.md`「全局层先做跨城分天、日内层再排序」是正解，但**未落到任何具体版本**。
- **结论：目前无已规划版本明确解决本问题。**

**建议修复方向（登记，未在 v1.3 实现）**：

- A（快修）：分天从 round-robin 改为**连续分块（contiguous chunk）**，Day k 取顺序中的连续一段，保留聚类；
- B（架构）：**cluster-then-assign**——全局先按城市簇分天，再日内排序，对齐终极构想六层架构；
- C（度量）：把打分与校验对象从「线性顺序」改为「**按日分组后**」的跨城次数（把天边界纳入度量），并下调 `CROSS_CITY_CONFLICT` 阈值或新增「可通过重分天降低跨城」的软校验，触发 `swap_neighbor` / 重新分天。

**建议归属**：v1.4 顺带把分天与打分口径改为「按日」；或作为 **v1.3.2 快修**（仅改分桶为连续分块，成本最低、收益直接）。

**修复状态（2026-08-10，v1.3.2 快修，已采纳方向 A）**：

- `agent-planner.buildPlanDataFromOrder` 分天算法由 `index % days` 轮询分桶改为**连续分块**：按顺序把景点切成 `days` 段，余数前置（前 `remainder` 天各多分 1 个点），使聚类后的同城景点落在同一天。
- 上文哥本哈根+马尔默 2 天例子已恢复为 Day1 全哥本哈根、Day2 全马尔默，**同日跨城降为 0**。
- 新增测试 `tests/agent-planner.test.js`：`keeps clustered order contiguous per day (OI-1)`（聚类不被打散）与 `splits unevenly with remainder front-loaded`（不均分时余数前置）；全量 `node --test` 通过（67/67）。
- **仍未做（留待 v1.4）**：方向 B（cluster-then-assign 全局分天）与方向 C（打分/校验改「按日分组」口径 + `CROSS_CITY_CONFLICT` 阈值下调）。本次快修依赖「传入顺序已按城市聚类」；当默认策略未聚类时，仍建议由 v1.4 从打分口径根治。

---

*本文档随讨论迭代；P0/P1/P2 已于 2026-08-10 落地，v1.3.1 路书重构同日完成；OI-1 登记为待解决问题。*

修改时间：2026-08-10 16:10 (UTC+8)
